def solution(keymap, targets):
    answer = []
    count = 0
    x = [0 for i in range(len(keymap))]
    print(x)
    
    for i in range(len(targets)):
        try:
            
        except ValueError:
    
    
    return answer
